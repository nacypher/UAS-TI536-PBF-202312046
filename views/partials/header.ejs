<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Daily Activity App' %></title>
    
    <link rel="icon" type="image/png" href="/images/DailyActivity.png">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

    <nav class="navbar navbar-expand-lg fixed-top shadow-sm">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center gap-2" href="/">
                
                <img src="/images/DailyActivity.png" alt="Logo" height="35" class="d-inline-block align-text-top">
                
                <span>Daily<span class="fw-bold">Activity</span></span>
            </a>
            
            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center gap-3">
                    <% if (typeof user !== 'undefined' && user) { %>
                        <li class="nav-item">
                            <span class="nav-link text-dark fw-medium">Halo, <%= user.nama_lengkap.split(' ')[0] %>! 👋</span>
                        </li>
                        <li class="nav-item">
                            <form action="/auth/logout" method="POST" class="d-inline">
                                <button class="btn btn-danger btn-sm rounded-pill px-4" type="submit">
                                    Logout <i class="fas fa-sign-out-alt ms-1"></i>
                                </button>
                            </form>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link fw-medium" href="/auth/login">Login</a>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-primary btn-sm rounded-pill px-4 text-white" href="/auth/register">Daftar Sekarang</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div style="height: 120px;"></div>